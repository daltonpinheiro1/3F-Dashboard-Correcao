/**
 * Meta mensal portados — espelho client-side (mesma lógica que functions/_lib).
 */
export {
  DEFAULT_META_PORTADOS_PCT,
  pctMeta,
  resolveMetaPortados,
  type MetaMes,
} from '../../functions/_lib/portabilidadeMeta';

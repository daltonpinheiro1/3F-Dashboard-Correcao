import { AdvertenciasWorkspace } from './AdvertenciasPage';

/** Ambiente do DP: aprovar, recusar, impressão e confirmação de entrega. */
export function ControleDpPage() {
  return <AdvertenciasWorkspace mode="dp" />;
}

export default ControleDpPage;
